# Flask web framework
Flask>=3.0.0
Werkzeug>=3.0.0

# Google Cloud Vertex AI
google-cloud-aiplatform>=1.60.0
vertexai>=1.71.0

# PDF processing
PyMuPDF>=1.23.0
reportlab>=4.0.0

# Alternative PDF library (if needed as fallback)
fpdf2>=2.7.0

# Additional dependencies that might be needed
# (these are usually installed automatically with the above packages)
google-auth>=2.25.0
requests>=2.31.0

# Optional: for better development experience
python-dotenv>=1.0.0